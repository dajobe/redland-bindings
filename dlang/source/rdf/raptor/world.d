module rdf.raptor.world;

import rdf.auxiliary.handled_record;

enum Raptor_Flag_Type : char { LibxmlErrorSave = 1,
                               LibxmlStructuredErrorSave = 2,
                               URIInterning = 3,
                               WWWSkipInitFinish = 4 }

unittest {
    assert(1);
}

// TODO
