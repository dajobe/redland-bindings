Stopped adding const modifiers at raptor/uri.d

const qualifier of member function where appropriate.
Also scope?

Correct identifiers case.

More unittests (especially for streams and iterators) and code review.

Review usage of const and cast(Dummy*) in handled_record.d.

